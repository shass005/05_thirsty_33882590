<!doctype html>
<html>
  <head>
    <title><%=shopName%></title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <h1>About <%=shopName%> Shop</h1>
    <p>We sell all sorts of drinks. You can find our stores at:</p>
    <ul><!--for loop goes over the content in stores-->
       <% for (let i = 0; i < stores.length; i++) { %>
        <li><!--for each element it displays the manager of the store and the address-->
          Manager: <%= stores[i].manager %><br>
          Address: <%= stores[i].address %>
        </li>
      <% } %>
    </ul>
  </body>
</html>
